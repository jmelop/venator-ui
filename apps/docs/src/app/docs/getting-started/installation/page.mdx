import { CodeBlock } from '@venator/ui';
import { PageHeader } from '@venator/patterns';

<PageHeader
  title="Installation"
  description="Get started with Venator in a new or existing React project."
/>

## Requirements

- **Node.js** >= 18
- **React** 18

Venator is optimised for use with Next.js 14+ (App Router).

## Install packages

<CodeBlock
  language="bash"
  filename="Terminal"
  code={`npm install @venator/ui @venator/patterns`}
/>

`@venator/tokens` is included as a transitive dependency. You can also install it directly if you need advanced access — for example, to use the Tailwind preset or design tokens in your own config.

## Configure Tailwind

<CodeBlock
  language="js"
  filename="tailwind.config.js"
  code={`const { venatorPreset } = require('@venator/tokens');

module.exports = {
  darkMode: 'class',
  presets: [venatorPreset],
  content: [
    './src/**\/*.{ts,tsx,md,mdx}',
    './node_modules/@venator/ui/dist/**\/*.{js,mjs}',
    './node_modules/@venator/patterns/dist/**\/*.{js,mjs}',
  ],
  theme: { extend: {} },
  plugins: [],
};`}
/>

## Add ToastProvider

<CodeBlock
  language="tsx"
  filename="app/layout.tsx"
  code={`import { ToastProvider } from '@venator/ui';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <ToastProvider>{children}</ToastProvider>
      </body>
    </html>
  );
}`}
/>

## First component

<CodeBlock
  language="tsx"
  filename="page.tsx"
  code={`import { Button, Card, CardContent, CardHeader } from '@venator/ui';

export default function Page() {
  return (
    <Card>
      <CardHeader title="Welcome to Venator" description="Start building." />
      <CardContent className="mt-4">
        <Button variant="primary">Get started</Button>
      </CardContent>
    </Card>
  );
}`}
/>
