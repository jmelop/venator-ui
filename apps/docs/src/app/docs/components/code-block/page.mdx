import { CodeBlock, Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@venator-ui/ui';
import { PageHeader } from '@venator-ui/patterns';

<PageHeader
  title="CodeBlock"
  description="Displays formatted code with syntax highlighting and copy to clipboard."
/>

## Basic

Renders a dark code block with a copy-to-clipboard button. The default language is `tsx`.

<CodeBlock
  code={`import { Button } from '@venator-ui/ui';

export function Example() {
  return <Button variant="primary">Click me</Button>;
}`}
/>

<CodeBlock className="mt-6"
  language="tsx"
  code={`<CodeBlock className="mt-6"
  code={\`import { Button } from '@venator-ui/ui';

export function Example() {
  return <Button variant="primary">Click me</Button>;
}\`}
/>`}
/>

## With filename

Pass `filename` to show a label bar above the code. The top corners of the code block become square to connect with the bar.

<CodeBlock
  filename="Button.tsx"
  code={`export interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  disabled?: boolean;
  isLoading?: boolean;
}`}
/>

<CodeBlock className="mt-6"
  language="tsx"
  code={`<CodeBlock className="mt-6"
  filename="Button.tsx"
  code={\`export interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  disabled?: boolean;
  isLoading?: boolean;
}\`}
/>`}
/>

## Bash

Set `language="bash"` for shell commands.

<CodeBlock
  language="bash"
  filename="Terminal"
  code={`npm install @venator-ui/ui @venator-ui/patterns @venator-ui/tokens`}
/>

<CodeBlock className="mt-6"
  language="tsx"
  code={`<CodeBlock className="mt-6"
  language="bash"
  filename="Terminal"
  code={\`npm install @venator-ui/ui @venator-ui/patterns @venator-ui/tokens\`}
/>`}
/>

## Props

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Prop</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Description</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">code</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">string</code></TableCell>
      <TableCell>—</TableCell>
      <TableCell>The source code string to display</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">language</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">string</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">'tsx'</code></TableCell>
      <TableCell>Language hint applied as a language-* class on &lt;code&gt;</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">filename</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">string</code></TableCell>
      <TableCell>—</TableCell>
      <TableCell>When provided, renders a filename bar above the code block</TableCell>
    </TableRow>
  </TableBody>
</Table>
