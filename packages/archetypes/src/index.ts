/**
 * @venator/archetypes
 *
 * Complete application architectures deployed via the Venator CLI.
 * Each archetype is a self-contained scaffold that is copied into the
 * developer's project — not consumed as a runtime dependency.
 */
