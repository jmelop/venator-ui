import { Toggle, CodeBlock, Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@venator-ui/ui';
import { PageHeader } from '@venator-ui/patterns';
import { Preview } from '@/components/Preview';
import { ToggleExample } from '@/components/ToggleExample';

<PageHeader
  title="Toggle"
  description="A pressable button that alternates between on and off."
/>

## Basic

`Toggle` manages pressed state internally when used uncontrolled via `defaultPressed`. Use `onPressedChange` to react to state changes.

<Preview>
  <ToggleExample />
</Preview>

<CodeBlock className="mt-6"
  language="tsx"
  code={`'use client';

import { useState } from 'react';
import { Toggle } from '@venator-ui/ui';

export function ToggleExample() {
  const [pressed, setPressed] = useState(false);

  return (
    <div className="flex items-center gap-3">
      <Toggle pressed={pressed} onPressedChange={setPressed}>
        Bold
      </Toggle>
      <span className="text-sm text-neutral-500">
        {pressed ? 'On' : 'Off'}
      </span>
    </div>
  );
}`}
/>

## Sizes

Use the `size` prop to control padding and font size.

<Preview>
  <div className="flex gap-3 flex-wrap items-center">
    <Toggle size="sm">Small</Toggle>
    <Toggle size="md">Medium</Toggle>
    <Toggle size="lg">Large</Toggle>
  </div>
</Preview>

<CodeBlock className="mt-6"
  language="tsx"
  code={`<Toggle size="sm">Small</Toggle>
<Toggle size="md">Medium</Toggle>
<Toggle size="lg">Large</Toggle>`}
/>

## Disabled

Set `disabled` to prevent interaction and apply muted styles.

<Preview>
  <div className="flex gap-3 flex-wrap items-center">
    <Toggle disabled>Disabled off</Toggle>
    <Toggle defaultPressed disabled>Disabled on</Toggle>
  </div>
</Preview>

<CodeBlock className="mt-6"
  language="tsx"
  code={`<Toggle disabled>Disabled off</Toggle>
<Toggle defaultPressed disabled>Disabled on</Toggle>`}
/>

## Props

### Toggle

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Prop</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Description</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">pressed</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">boolean</code></TableCell>
      <TableCell>—</TableCell>
      <TableCell>Controlled pressed state</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">defaultPressed</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">boolean</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">false</code></TableCell>
      <TableCell>Initial pressed state in uncontrolled mode</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">onPressedChange</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">(pressed: boolean) =&gt; void</code></TableCell>
      <TableCell>—</TableCell>
      <TableCell>Called when pressed state changes</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">size</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">'sm' | 'md' | 'lg'</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">'md'</code></TableCell>
      <TableCell>Controls padding and font size</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">disabled</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">boolean</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">false</code></TableCell>
      <TableCell>Prevents interaction and applies muted styles</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">className</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">string</code></TableCell>
      <TableCell>—</TableCell>
      <TableCell>Additional Tailwind classes</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">children</code></TableCell>
      <TableCell><code className="text-xs font-mono bg-neutral-100 dark:bg-neutral-800 px-1.5 py-0.5 rounded">ReactNode</code></TableCell>
      <TableCell>—</TableCell>
      <TableCell>Label or icon content</TableCell>
    </TableRow>
  </TableBody>
</Table>
